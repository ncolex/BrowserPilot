services:
  - type: web
    name: browserpilot
    runtime: docker
    plan: free
    region: ohio
    healthCheck:
      path: /
      port: 8000
    envGroup: browserpilot-secrets
    dockerContext: .
    dockerfilePath: ./Dockerfile
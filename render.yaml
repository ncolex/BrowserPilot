services:
  - type: web
    name: browserpilot
    runtime: docker
    plan: starter
    region: ohio
    healthCheck:
      path: /
      port: 8000
    envGroup: browserpilot-secrets
    dockerContext: .
    dockerfilePath: ./Dockerfile